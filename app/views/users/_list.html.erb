<!--部分テンプレートにインスタンス変数は使用しない-->
<!--投稿フォーム-->
<h2 class="mt-3">New book</h2>

<!--エラー表示-->
<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
  <li><%= message %></li>
  <% end %>
</ul>
<% end %>

<!--入力フォームや送信ボタン作るためのヘルパーメソッド-->
<%= form_with model: book do |f| %> 

<!--タイトル入力-->
<div class ="field">
  <%= f.label :title %><!--最初の先頭の文字がTitle<label for="book_title">Title</label>-->
  <%= f.text_field :title %>
</div>

<div class ="field">
  <%= f.label :Opinion %><!--最初の先頭の文字がTitle<label for="book_title">Title</label>-->
  <%= f.text_area :body %>
</div>

<div class="actions">
  <%= f.submit 'Create Book', class: "btn btn-success" %>
</div>
  <% end %>